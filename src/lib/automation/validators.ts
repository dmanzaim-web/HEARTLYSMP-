import { z } from "zod";
export const automationConditionSchema=z.object({field:z.string().min(1).max(64),operator:z.enum(["equals","not_equals","contains","not_contains","starts_with","ends_with","greater_than","less_than"]),value:z.string().max(500),position:z.number().int().min(0).optional()});
export const automationActionSchema=z.object({type:z.enum(["SEND_CHAT","EXECUTE_COMMAND","SEND_NOTIFICATION","DELAY","START_BOT","STOP_BOT","RESTART_BOT"]),value:z.string().max(1000).optional(),delayMs:z.number().int().min(0).max(60000).optional(),position:z.number().int().min(0).optional()});
export const automationSchema=z.object({name:z.string().trim().min(2).max(100),trigger:z.string().min(1),enabled:z.boolean().optional(),cooldownSeconds:z.number().int().min(0).max(86400).optional(),executionLimit:z.number().int().min(1).nullable().optional(),notifyOnFailure:z.boolean().optional(),conditions:z.array(automationConditionSchema).max(20),actions:z.array(automationActionSchema).max(20),scheduleType:z.string().max(32).nullable().optional(),scheduleValue:z.string().max(100).nullable().optional()});
